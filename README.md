# To build the image run:
` sudo docker build -t devops-bot . `
# To run the image:
` sudo docker run -p 8501:8501 devops-bot `

Open https://localhost:8501 to access the devops-bot. 



# AI Disclosure:
Parts of my workflow were assisted by Microsoft Copilot, specifically for verifying syntax, troubleshooting Docker build errors, and resolving GitHub connection issues. All project code (including the Dockerfile, dev.py, and repository setup) was written, reviewed, and confirmed by me. I understand how the Dockerfile works and can explain each step of the build and run process.
